## IDF Component Manager Manifest File
dependencies:
  espressif/tinyusb: 
     version: ^0.18.0~2
     rules:
      - if: "target in [esp32s2, esp32s3, esp32p4]"
  idf: ^5.0
  espressif/esp_tinyusb: 
     version: "^1.4.2"
     rules:
      - if: "target in [esp32s2, esp32s3, esp32p4]"